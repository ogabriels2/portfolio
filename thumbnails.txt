<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#fbfbfd">
  <title>Thumbnails | Gabriel Silva</title>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    /* ---------------- Design System ---------------- */
    :root {
      --sidebar-width: 280px;
      --max-width: 1100px; /* Largura um pouco maior para o grid */
      
      /* Cores */
      --bg-body: #F2F2F7; /* Fundo ligeiramente mais escuro para destacar as imagens */
      --bg-sidebar: rgba(255, 255, 255, 0.85);
      --bg-card: #FFFFFF;
      --text-primary: #1D1D1F;
      --text-secondary: #86868B;
      --accent: #FF0000; /* Vermelho YouTube como destaque */
      
      /* Efeitos */
      --shadow-sm: 0 4px 12px rgba(0, 0, 0, 0.05);
      --shadow-hover: 0 12px 32px rgba(0, 0, 0, 0.15);
      --glass-blur: blur(20px) saturate(180%);
      --radius-lg: 24px;
      --radius-md: 12px; /* Cantos mais nítidos para as thumbs no grid */
      --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    
    body {
      font-family: 'Inter', sans-serif;
      color: var(--text-primary);
      background-color: var(--bg-body);
      -webkit-font-smoothing: antialiased;
      display: flex;
      min-height: 100vh;
    }

    /* --- Sidebar --- */
    aside {
      width: var(--sidebar-width); height: 100vh; position: fixed; left: 0; top: 0;
      padding: 60px 32px; background: var(--bg-sidebar);
      backdrop-filter: var(--glass-blur); -webkit-backdrop-filter: var(--glass-blur);
      border-right: 1px solid rgba(0,0,0,0.05);
      display: flex; flex-direction: column; z-index: 50;
    }

    .profile-container { text-align: center; margin-bottom: 40px; }
    .profile-pic-wrapper { width: 120px; height: 120px; margin: 0 auto 20px; border-radius: 50%; overflow: hidden; box-shadow: var(--shadow-sm); transition: transform 0.3s ease; }
    .profile-pic-wrapper:hover { transform: scale(1.03); }
    .profile-pic { width: 100%; height: 100%; object-fit: cover; }
    .profile-name { font-size: 1.5rem; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 6px; color: #111; }
    .profile-role { font-size: 0.8rem; color: var(--text-secondary); font-weight: 500; text-transform: uppercase; letter-spacing: 0.05em; }

    nav { flex: 1; display: flex; flex-direction: column; gap: 24px; }
    .nav-group h3 { font-size: 0.75rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 12px; opacity: 0.8; }
    .nav-list { list-style: none; }
    .nav-list li { margin-bottom: 4px; }
    .nav-link { display: block; padding: 8px 12px; color: var(--text-secondary); text-decoration: none; font-size: 0.95rem; font-weight: 500; border-radius: 8px; transition: all 0.2s; }
    .nav-link:hover { color: var(--accent); background: rgba(255, 0, 0, 0.05); }
    
    .sidebar-footer { margin-top: auto; padding-top: 20px; border-top: 1px solid rgba(0,0,0,0.05); }
    .social-icons { display: flex; justify-content: center; gap: 12px; margin-bottom: 16px; }
    .social-icon { width: 36px; height: 36px; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(0,0,0,0.04); color: var(--text-secondary); text-decoration: none; transition: all 0.3s; }
    .social-icon:hover { background: var(--accent); color: white; transform: translateY(-3px); }
    .copyright { font-size: 0.7rem; color: #ccc; text-align: center; }

    /* --- Project Content --- */
    main {
      margin-left: var(--sidebar-width);
      flex: 1; padding: 0;
      display: flex; flex-direction: column; align-items: center;
    }

    /* Hero Section (Para a melhor thumb) */
    .project-hero {
      width: 100%; height: 65vh; min-height: 450px; position: relative; background-color: #111; overflow: hidden;
    }

    .hero-image {
      width: 100%; height: 100%; object-fit: cover;
      opacity: 0.9;
      cursor: zoom-in; transition: transform 0.5s ease;
    }
    .hero-image:hover { transform: scale(1.02); }

    .hero-content {
      position: absolute; bottom: 0; left: 0; width: 100%;
      padding: 60px;
      background: linear-gradient(to top, rgba(0,0,0,0.9), transparent);
      pointer-events: none;
    }

    .container { width: 100%; max-width: var(--max-width); margin: 0 auto; }
    
    .project-title { font-size: 3.5rem; font-weight: 700; color: white; margin-bottom: 10px; letter-spacing: -0.03em; }
    .project-subtitle { font-size: 1.2rem; color: rgba(255,255,255,0.9); font-weight: 400; }

    .content-wrapper { padding: 60px; width: 100%; max-width: var(--max-width); }

    .meta-grid {
      display: grid; grid-template-columns: repeat(3, 1fr);
      gap: 24px; margin-bottom: 60px;
      background: white; padding: 32px; border-radius: var(--radius-lg);
      box-shadow: var(--shadow-sm);
    }
    .meta-item h4 { font-size: 0.8rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 8px; }
    .meta-item p { font-size: 1.1rem; font-weight: 600; color: var(--text-primary); }

    .project-description { font-size: 1.25rem; line-height: 1.7; color: #444; margin-bottom: 80px; }
    .project-description strong { color: var(--accent); font-weight: 600; }

    /* --- THUMBNAIL GRID --- */
    .showcase-section { margin-bottom: 80px; }
    .section-header { margin-bottom: 40px; text-align: center; }
    .section-title { font-size: 2rem; font-weight: 700; margin-bottom: 8px; }
    .section-desc { color: var(--text-secondary); font-size: 1.1rem; }

    .thumb-grid {
      display: grid;
      /* Grid responsivo: cria colunas de no mínimo 300px, preenchendo o espaço */
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 24px;
    }

    .thumb-card {
      position: relative;
      border-radius: var(--radius-md);
      overflow: hidden;
      aspect-ratio: 16/9; /* Proporção exata de YouTube */
      box-shadow: var(--shadow-sm);
      transition: var(--transition);
      cursor: zoom-in;
      background: #e0e0e0; /* Placeholder enquanto carrega */
    }

    .thumb-card:hover {
      transform: translateY(-5px) scale(1.02);
      box-shadow: var(--shadow-hover);
      z-index: 2;
    }

    .thumb-img {
      width: 100%; height: 100%; object-fit: cover; display: block;
    }

    /* Overlay de "Play" no hover (Opcional, mas elegante) */
    .thumb-card::after {
      content: '\f04b'; /* FontAwesome Play Icon */
      font-family: 'Font Awesome 6 Free'; font-weight: 900;
      position: absolute;
      inset: 0;
      background: rgba(0,0,0,0.3);
      display: flex; align-items: center; justify-content: center;
      color: white; font-size: 2rem;
      opacity: 0; transition: var(--transition);
    }
    .thumb-card:hover::after { opacity: 1; }

    /* Navegação */
    .project-nav {
      width: 100%; padding: 60px;
      border-top: 1px solid rgba(0,0,0,0.1);
      display: flex; justify-content: space-between; align-items: center;
    }
    .nav-btn {
      text-decoration: none; display: flex; align-items: center; gap: 12px;
      color: var(--text-secondary); font-weight: 500; transition: 0.3s;
    }
    .nav-btn:hover { color: var(--accent); }
    .nav-btn.next { text-align: right; flex-direction: row-reverse; }
    .nav-label { font-size: 0.8rem; text-transform: uppercase; display: block; margin-bottom: 4px; opacity: 0.6; }
    .nav-project-name { font-size: 1.2rem; font-weight: 700; color: var(--text-primary); }

    /* Lightbox */
    .lightbox {
      position: fixed; inset: 0; z-index: 1000;
      background: rgba(0, 0, 0, 0.95);
      backdrop-filter: blur(20px);
      display: flex; justify-content: center; align-items: center;
      opacity: 0; pointer-events: none; transition: opacity 0.3s ease;
    }
    .lightbox.active { opacity: 1; pointer-events: auto; }
    .lb-image { max-width: 90vw; max-height: 90vh; border-radius: 8px; box-shadow: 0 20px 60px rgba(0,0,0,0.5); }
    .lb-control {
      position: absolute; background: rgba(255, 255, 255, 0.1); color: white;
      border: none; width: 48px; height: 48px; border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      font-size: 1.2rem; cursor: pointer; transition: 0.2s;
    }
    .lb-control:hover { background: rgba(255, 255, 255, 0.25); }
    .lb-close { top: 20px; right: 20px; }

    @media (max-width: 968px) {
      body { flex-direction: column; }
      aside { width: 100%; height: auto; position: relative; padding: 20px; border-right: none; flex-direction: row; justify-content: space-between; align-items: center; }
      .profile-role, nav, .sidebar-footer { display: none; }
      .profile-container { margin: 0; text-align: left; display: flex; align-items: center; gap: 12px; }
      .profile-pic-wrapper { width: 48px; height: 48px; margin: 0; }
      .profile-name { font-size: 1.1rem; margin: 0; }
      main { margin-left: 0; }
      .project-title { font-size: 2.5rem; }
      .meta-grid { grid-template-columns: 1fr; gap: 20px; }
      .content-wrapper { padding: 40px 20px; }
      .thumb-grid { grid-template-columns: 1fr; } /* Uma coluna no mobile */
    }
  </style>
</head>
<body>

  <aside>
    <div class="profile-container">
      <div class="profile-pic-wrapper">
        <img src="Logo.png" alt="Gabriel Silva" class="profile-pic">
      </div>
      <h1 class="profile-name">Gabriel Silva</h1>
      <p class="profile-role">Designer & Produtor de Vídeo</p>
    </div>

    <nav>
      <div class="nav-group">
        <ul class="nav-list">
          <li><a href="sobre.html" class="nav-link">Sobre mim</a></li>
        </ul>
      </div>

      <div class="nav-group">
        <h3>Portfólio</h3>
        <ul class="nav-list">
          <li><a href="index.html" class="nav-link active">← Voltar para Projetos</a></li>
        </ul>
      </div>
    </nav>

    <div class="sidebar-footer">
      <div class="social-icons">
        <a href="https://www.linkedin.com/in/ogabriels/" target="_blank" class="social-icon"><i class="fab fa-linkedin-in"></i></a>
        <a href="https://www.instagram.com/ogabriels0/" target="_blank" class="social-icon"><i class="fab fa-instagram"></i></a>
        <a href="mailto:contato@ogabriels.com" class="social-icon"><i class="far fa-envelope"></i></a>
        <a href="https://wa.me/5511979686116" target="_blank" class="social-icon"><i class="fab fa-whatsapp"></i></a>
      </div>
      <p class="copyright">© 2026 Gabriel Silva</p>
    </div>
  </aside>

  <main>
    
    <section class="project-hero">
      <img src="SUA_MELHOR_THUMBNAIL_AQUI.jpg" alt="Destaque Thumbnail" class="hero-image lightbox-trigger">
      
      <div class="hero-content">
        <div class="container">
          <h1 class="project-title">Thumbnails</h1>
          <p class="project-subtitle">Design Estratégico para YouTube</p>
        </div>
      </div>
    </section>

    <div class="content-wrapper">
      
      <section class="meta-grid">
        <div class="meta-item"><h4>Projeto</h4><p>Design para Social Media</p></div>
        <div class="meta-item"><h4>Foco</h4><p>Aumento de CTR</p></div>
        <div class="meta-item"><h4>Skills</h4><p>Composição & Tipografia</p></div>
      </section>

      <section class="project-description">
        <p>Crio thumbnails para YouTube, desenvolvendo composições visuais atrativas que aumentam a taxa de cliques (CTR) de vídeos. Meu trabalho envolve uma combinação estratégica de <strong>edição de imagens, escolha de tipografia, manipulação de cores e composição.</strong></p>
        <p>O objetivo não é apenas fazer uma imagem bonita, mas criar um elemento de design funcional que destaque o conteúdo em meio à concorrência, desperte a curiosidade e capte a atenção do público em frações de segundo.</p>
      </section>

      <section class="showcase-section">
        <div class="section-header">
          <h2 class="section-title">Galeria de Projetos</h2>
          <p class="section-desc">Uma seleção de capas desenvolvidas para diferentes nichos e estratégias.</p>
        </div>
        
        <div class="thumb-grid">
          <div class="thumb-card">
            <img src="THUMB_EXEMPLO_1.jpg" alt="Thumbnail 1" class="thumb-img lightbox-trigger">
          </div>

          <div class="thumb-card">
            <img src="THUMB_EXEMPLO_2.jpg" alt="Thumbnail 2" class="thumb-img lightbox-trigger">
          </div>

          <div class="thumb-card">
            <img src="THUMB_EXEMPLO_3.jpg" alt="Thumbnail 3" class="thumb-img lightbox-trigger">
          </div>

          <div class="thumb-card">
            <img src="THUMB_EXEMPLO_4.jpg" alt="Thumbnail 4" class="thumb-img lightbox-trigger">
          </div>

          <div class="thumb-card">
            <img src="THUMB_EXEMPLO_5.jpg" alt="Thumbnail 5" class="thumb-img lightbox-trigger">
          </div>

          <div class="thumb-card">
            <img src="THUMB_EXEMPLO_6.jpg" alt="Thumbnail 6" class="thumb-img lightbox-trigger">
          </div>
          
          </div>
      </section>

    </div>

    <div class="project-nav">
      <a href="valorada.html" class="nav-btn prev">
        <i class="fa-solid fa-arrow-left"></i>
        <div><span class="nav-label">Anterior</span><span class="nav-project-name">Valorada</span></div>
      </a>
      
      <a href="index.html" class="nav-btn next">
        <div><span class="nav-label">Início</span><span class="nav-project-name">Voltar à Home</span></div>
        <i class="fa-solid fa-house"></i>
      </a>
    </div>

  </main>

  <div id="lightbox" class="lightbox">
    <button id="lb-close" class="lb-control lb-close"><i class="fa-solid fa-xmark"></i></button>
    <div class="lb-image-container">
      <img src="" alt="Zoom" id="lb-image" class="lb-image">
    </div>
  </div>

  <script>
    const triggers = document.querySelectorAll('.lightbox-trigger');
    const lightbox = document.getElementById('lightbox');
    const lbImage = document.getElementById('lb-image');
    const lbClose = document.getElementById('lb-close');

    function openLightbox(src) {
      lbImage.src = src;
      lightbox.classList.add('active');
      document.body.style.overflow = 'hidden';
    }

    function closeLightbox() {
      lightbox.classList.remove('active');
      document.body.style.overflow = '';
      setTimeout(() => { lbImage.src = ''; }, 300);
    }

    triggers.forEach(img => {
      img.addEventListener('click', () => openLightbox(img.src));
    });

    lbClose.addEventListener('click', closeLightbox);
    lightbox.addEventListener('click', (e) => {
      if(e.target === lightbox) closeLightbox();
    });
    document.addEventListener('keydown', (e) => {
      if(e.key === 'Escape') closeLightbox();
    });
  </script>

</body>
</html>