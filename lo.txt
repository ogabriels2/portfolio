<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#fbfbfd">
  <title>Lô Publicidade | Gabriel Silva</title>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    /* ---------------- Design System ---------------- */
    :root {
      --sidebar-width: 280px;
      --max-width: 1000px;
      
      /* Cores */
      --bg-body: #F5F5F7;
      --bg-sidebar: rgba(255, 255, 255, 0.75);
      --bg-card: #FFFFFF;
      --text-primary: #1D1D1F;
      --text-secondary: #86868B;
      --accent: #0071E3; /* Azul Link */
      --brand-color: #024B47; /* Verde Lô para detalhes específicos */
      
      /* Efeitos */
      --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.04);
      --shadow-md: 0 8px 24px rgba(0, 0, 0, 0.06);
      --glass-blur: blur(20px) saturate(180%);
      --radius-lg: 24px;
      --transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
    }

    /* Reset */
    * { box-sizing: border-box; margin: 0; padding: 0; }
    
    body {
      font-family: 'Inter', sans-serif;
      color: var(--text-primary);
      background-color: var(--bg-body);
      -webkit-font-smoothing: antialiased;
      display: flex;
      min-height: 100vh;
    }

    /* --- Sidebar --- */
    aside {
      width: var(--sidebar-width); height: 100vh; position: fixed; left: 0; top: 0;
      padding: 60px 32px; background: var(--bg-sidebar);
      backdrop-filter: var(--glass-blur); -webkit-backdrop-filter: var(--glass-blur);
      border-right: 1px solid rgba(0,0,0,0.05);
      display: flex; flex-direction: column; z-index: 50;
    }

    .profile-container { text-align: center; margin-bottom: 40px; }
    .profile-pic-wrapper { width: 120px; height: 120px; margin: 0 auto 20px; border-radius: 50%; overflow: hidden; box-shadow: var(--shadow-md); transition: transform 0.3s ease; }
    .profile-pic-wrapper:hover { transform: scale(1.03); }
    .profile-pic { width: 100%; height: 100%; object-fit: cover; }
    .profile-name { font-size: 1.5rem; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 6px; color: #111; }
    .profile-role { font-size: 0.8rem; color: var(--text-secondary); font-weight: 500; text-transform: uppercase; letter-spacing: 0.05em; }

    nav { flex: 1; display: flex; flex-direction: column; gap: 24px; }
    .nav-group h3 { font-size: 0.75rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 12px; opacity: 0.8; }
    .nav-list { list-style: none; }
    .nav-list li { margin-bottom: 4px; }
    .nav-link { display: block; padding: 8px 12px; color: var(--text-secondary); text-decoration: none; font-size: 0.95rem; font-weight: 500; border-radius: 8px; transition: all 0.2s; }
    .nav-link:hover { color: var(--accent); background: rgba(0, 113, 227, 0.08); }
    
    .sidebar-footer { margin-top: auto; padding-top: 20px; border-top: 1px solid rgba(0,0,0,0.05); }
    .social-icons { display: flex; justify-content: center; gap: 12px; margin-bottom: 16px; }
    .social-icon { width: 36px; height: 36px; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(0,0,0,0.04); color: var(--text-secondary); text-decoration: none; transition: all 0.3s; }
    .social-icon:hover { background: var(--accent); color: white; transform: translateY(-3px); }
    .copyright { font-size: 0.7rem; color: #ccc; text-align: center; }

    /* --- Project Content --- */
    main {
      margin-left: var(--sidebar-width);
      flex: 1; padding: 0;
      display: flex; flex-direction: column; align-items: center;
    }

    .project-hero {
      width: 100%; height: 60vh; min-height: 400px; position: relative; background-color: #024B47; overflow: hidden;
    }

    .hero-image {
      width: 100%; height: 100%; object-fit: cover;
      opacity: 0.9;
      mask-image: linear-gradient(to bottom, black 80%, transparent 100%);
      -webkit-mask-image: linear-gradient(to bottom, black 80%, transparent 100%);
      cursor: zoom-in; transition: transform 0.5s ease;
    }
    .hero-image:hover { transform: scale(1.02); }

    .hero-content {
      position: absolute; bottom: 0; left: 0; width: 100%;
      padding: 60px;
      background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
      pointer-events: none;
    }

    .container { width: 100%; max-width: var(--max-width); margin: 0 auto; }
    
    .project-title { font-size: 3.5rem; font-weight: 700; color: white; margin-bottom: 10px; letter-spacing: -0.03em; text-shadow: 0 4px 20px rgba(0,0,0,0.2); }
    .project-subtitle { font-size: 1.2rem; color: rgba(255,255,255,0.9); font-weight: 400; }

    .content-wrapper { padding: 60px; width: 100%; max-width: var(--max-width); }

    .meta-grid {
      display: grid; grid-template-columns: repeat(3, 1fr);
      gap: 24px; margin-bottom: 60px;
      background: white; padding: 32px; border-radius: var(--radius-lg);
      box-shadow: var(--shadow-sm);
    }
    
    .meta-item h4 { font-size: 0.8rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 8px; }
    .meta-item p { font-size: 1.1rem; font-weight: 600; color: var(--text-primary); }

    .project-description { font-size: 1.25rem; line-height: 1.7; color: #444; margin-bottom: 80px; }
    .project-description strong { color: #024B47; font-weight: 600; }

    .showcase-section { margin-bottom: 80px; }
    .section-header { margin-bottom: 32px; border-left: 4px solid var(--brand-color); padding-left: 16px; }
    .section-title { font-size: 1.8rem; font-weight: 700; margin-bottom: 8px; }
    .section-desc { color: var(--text-secondary); font-size: 1rem; }

    /* Imagens de Galeria */
    .full-width-img, .gallery-grid img {
      width: 100%; border-radius: var(--radius-lg);
      box-shadow: var(--shadow-md); margin-bottom: 32px;
      transition: transform 0.5s ease, box-shadow 0.5s ease;
      cursor: zoom-in;
    }
    .full-width-img:hover, .gallery-grid img:hover { transform: scale(1.02); box-shadow: 0 20px 40px rgba(0,0,0,0.12); }

    .gallery-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 32px; }
    .gallery-grid img { margin-bottom: 0; height: 100%; object-fit: cover; }

    /* Rodapé de Navegação */
    .project-nav {
      width: 100%; padding: 60px;
      border-top: 1px solid rgba(0,0,0,0.1);
      display: flex; justify-content: space-between; align-items: center;
    }
    .nav-btn {
      text-decoration: none; display: flex; align-items: center; gap: 12px;
      color: var(--text-secondary); font-weight: 500; transition: 0.3s;
    }
    .nav-btn:hover { color: var(--accent); }
    .nav-btn.next { text-align: right; flex-direction: row-reverse; }
    .nav-label { font-size: 0.8rem; text-transform: uppercase; display: block; margin-bottom: 4px; opacity: 0.6; }
    .nav-project-name { font-size: 1.2rem; font-weight: 700; color: var(--text-primary); }

    /* --- LIGHTBOX --- */
    .lightbox {
      position: fixed; inset: 0; z-index: 1000;
      background: rgba(0, 0, 0, 0.9);
      backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
      display: flex; justify-content: center; align-items: center;
      opacity: 0; pointer-events: none; transition: opacity 0.3s ease;
    }
    .lightbox.active { opacity: 1; pointer-events: auto; }
    
    .lb-image-container { position: relative; max-width: 90vw; max-height: 90vh; transform: scale(0.95); transition: transform 0.4s ease; }
    .lightbox.active .lb-image-container { transform: scale(1); }
    
    .lb-image { max-width: 100%; max-height: 90vh; display: block; border-radius: 4px; box-shadow: 0 20px 60px rgba(0,0,0,0.5); transition: opacity 0.2s ease; }
    .lb-image.fade-out { opacity: 0.5; }

    .lb-control {
      position: absolute; background: rgba(255, 255, 255, 0.1); color: white;
      border: none; width: 48px; height: 48px; border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      font-size: 1.2rem; cursor: pointer; transition: 0.2s; z-index: 10;
      backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
    }
    .lb-control:hover { background: rgba(255, 255, 255, 0.25); transform: scale(1.1); }
    .lb-close { top: 20px; right: 20px; } .lb-prev { left: 20px; } .lb-next { right: 20px; }

    @media (max-width: 968px) {
      body { flex-direction: column; }
      aside { width: 100%; height: auto; position: relative; padding: 20px; border-right: none; border-bottom: 1px solid rgba(0,0,0,0.1); flex-direction: row; justify-content: space-between; align-items: center; }
      .profile-role, nav, .sidebar-footer { display: none; }
      .profile-container { margin: 0; text-align: left; display: flex; align-items: center; gap: 12px; }
      .profile-pic-wrapper { width: 48px; height: 48px; margin: 0; }
      .profile-name { font-size: 1.1rem; margin: 0; }
      main { margin-left: 0; }
      .project-title { font-size: 2.5rem; }
      .meta-grid { grid-template-columns: 1fr; gap: 20px; }
      .content-wrapper { padding: 40px 20px; }
      .gallery-grid { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>

  <aside>
    <div class="profile-container">
      <div class="profile-pic-wrapper">
        <img src="Logo.png" alt="Gabriel Silva" class="profile-pic">
      </div>
      <h1 class="profile-name">Gabriel Silva</h1>
      <p class="profile-role">Designer & Produtor de Vídeo</p>
    </div>

    <nav>
      <div class="nav-group">
        <ul class="nav-list">
          <li><a href="sobre.html" class="nav-link">Sobre mim</a></li>
        </ul>
      </div>

      <div class="nav-group">
        <h3>Portfólio</h3>
        <ul class="nav-list">
          <li><a href="index.html" class="nav-link active">← Voltar para Projetos</a></li>
        </ul>
      </div>
    </nav>

    <div class="sidebar-footer">
      <div class="social-icons">
        <a href="https://www.linkedin.com/in/ogabriels/" target="_blank" class="social-icon"><i class="fab fa-linkedin-in"></i></a>
        <a href="https://www.instagram.com/ogabriels0/" target="_blank" class="social-icon"><i class="fab fa-instagram"></i></a>
        <a href="mailto:contato@ogabriels.com" class="social-icon"><i class="far fa-envelope"></i></a>
        <a href="https://wa.me/5511979686116" target="_blank" class="social-icon"><i class="fab fa-whatsapp"></i></a>
      </div>
      <p class="copyright">© 2026 Gabriel Silva</p>
    </div>
  </aside>

  <main>
    
    <section class="project-hero">
      <img src="Lô Publi - capa.png" alt="Capa Lô Publicidade" class="hero-image lightbox-trigger">
      <div class="hero-content">
        <div class="container">
          <h1 class="project-title">Lô</h1>
          <p class="project-subtitle">Branding, Identidade Visual e Comunicação</p>
        </div>
      </div>
    </section>

    <div class="content-wrapper">
      
      <section class="meta-grid">
        <div class="meta-item"><h4>Projeto</h4><p>Acadêmico</p></div>
        <div class="meta-item"><h4>Ano</h4><p>2022</p></div>
        <div class="meta-item"><h4>Área</h4><p>Branding</p></div>
      </section>

      <section class="project-description">
        <p>A <strong>Lô</strong> nasceu como um projeto de agência experimental com um propósito claro: comunicar a diversidade cultural brasileira através de uma linguagem acessível e sofisticada.</p>
        <p>A identidade visual funde a elegância clássica da <strong>Art Nouveau</strong> e <strong>Art Déco</strong> com uma paleta de cores quente e acolhedora. Fui responsável por toda a estruturação da marca, criando um sistema visual que é, ao mesmo tempo, premium e inclusivo, refletindo a pluralidade do Brasil em cada detalhe, da tipografia à papelaria.</p>
      </section>

      <section class="showcase-section">
        <div class="section-header">
          <h2 class="section-title">A Marca</h2>
          <p class="section-desc">Uma fusão de estilos clássicos com uma abordagem geométrica moderna.</p>
        </div>
        
        <div class="gallery-grid">
          <img src="Estrutura do logotipo Lô em preto e branco, ilustrando a combinação de símbolos que remetem à arte Nouveau e Déco.png" alt="Estrutura do Logotipo" class="lightbox-trigger">
          <img src="Logotipo principal do projeto Lô em cor creme sobre fundo verde, representando a identidade visual voltada à arte Nouveau e Déco.png" alt="Logotipo Principal Lô" class="lightbox-trigger">
        </div>
      </section>

      <section class="showcase-section">
        <div class="section-header">
          <h2 class="section-title">Sistema Visual</h2>
          <p class="section-desc">Tipografia elegante e uma paleta de cores inspirada na natureza brasileira.</p>
        </div>

        <div class="gallery-grid">
          <img src="Tipografia do projeto Lô, mostrando a combinação das fontes Raleway Regular e Leky Caligna para um estilo moderno e elegante.png" alt="Tipografia" class="lightbox-trigger">
          <img src="Paleta de cores do projeto Lô, exibindo tons como EEB331, 702F06, 59694F, 024B47 e A30119, além de branco e preto.png" alt="Paleta de Cores" class="lightbox-trigger">
        </div>
      </section>

      <section class="showcase-section">
        <div class="section-header">
          <h2 class="section-title">Aplicações</h2>
          <p class="section-desc">O universo da marca aplicado em papelaria e brindes de alto padrão.</p>
        </div>
        
        <img src="Conjunto de brindes premium do projeto Lô, incluindo garrafa de vinho e velas personalizadas em vermelho, reforçando a identidade visual.png" alt="Brindes Premium Lô" class="full-width-img lightbox-trigger">

        <div class="gallery-grid">
          <img src="Papelaria Premium.png" alt="Envelopes e Papelaria" class="lightbox-trigger">
          <img src="Pasta personalizada do projeto Lô em vermelho, com padronagens geométricas na parte superior, refletindo a identidade visual da marca.png" alt="Pasta Personalizada" class="lightbox-trigger">
        </div>
      </section>

    </div>

    <div class="project-nav">
      <a href="neo-letters.html" class="nav-btn prev">
        <i class="fa-solid fa-arrow-left"></i>
        <div><span class="nav-label">Anterior</span><span class="nav-project-name">Neo Letters</span></div>
      </a>
      
      <a href="g-gabriel.html" class="nav-btn next">
        <div><span class="nav-label">Próximo</span><span class="nav-project-name">G | Gabriel</span></div>
        <i class="fa-solid fa-arrow-right"></i>
      </a>
    </div>

  </main>

  <div id="lightbox" class="lightbox">
    <button id="lb-close" class="lb-control lb-close"><i class="fa-solid fa-xmark"></i></button>
    <button id="lb-prev" class="lb-control lb-prev"><i class="fa-solid fa-chevron-left"></i></button>
    <button id="lb-next" class="lb-control lb-next"><i class="fa-solid fa-chevron-right"></i></button>
    <div class="lb-image-container">
      <img src="" alt="Zoom" id="lb-image" class="lb-image">
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const triggers = document.querySelectorAll('.lightbox-trigger');
      const lightbox = document.getElementById('lightbox');
      const lbImage = document.getElementById('lb-image');
      const lbClose = document.getElementById('lb-close');
      const lbPrev = document.getElementById('lb-prev');
      const lbNext = document.getElementById('lb-next');

      let currentIndex = 0;
      const imagesList = Array.from(triggers).map(img => img.src);

      function openLightbox(index) {
        currentIndex = index;
        lbImage.src = imagesList[currentIndex];
        lightbox.classList.add('active');
        document.body.style.overflow = 'hidden';
      }

      function closeLightbox() {
        lightbox.classList.remove('active');
        document.body.style.overflow = '';
        setTimeout(() => { lbImage.src = ''; }, 300);
      }

      function changeImage(direction) {
        currentIndex = (currentIndex + direction + imagesList.length) % imagesList.length;
        lbImage.classList.add('fade-out');
        setTimeout(() => {
            lbImage.src = imagesList[currentIndex];
            lbImage.classList.remove('fade-out');
        }, 150);
      }

      triggers.forEach((trigger, index) => {
        trigger.addEventListener('click', (e) => { e.stopPropagation(); openLightbox(index); });
      });

      lbClose.addEventListener('click', closeLightbox);
      lbPrev.addEventListener('click', (e) => { e.stopPropagation(); changeImage(-1); });
      lbNext.addEventListener('click', (e) => { e.stopPropagation(); changeImage(1); });

      lightbox.addEventListener('click', (e) => {
        if (e.target === lightbox || e.target.classList.contains('lb-image-container')) closeLightbox();
      });

      document.addEventListener('keydown', (e) => {
        if (!lightbox.classList.contains('active')) return;
        if (e.key === 'Escape') closeLightbox();
        if (e.key === 'ArrowLeft') changeImage(-1);
        if (e.key === 'ArrowRight') changeImage(1);
      });
    });
  </script>

</body>
</html>