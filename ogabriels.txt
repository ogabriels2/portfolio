<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#fbfbfd">
  <title>G | Gabriel - Personal Branding</title>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    /* ---------------- Design System (Customizado para a Marca G) ---------------- */
    :root {
      --sidebar-width: 280px;
      --max-width: 1000px;
      
      /* Cores Específicas deste Projeto */
      --bg-body: #F9F9F9; /* Cinza ultra leve */
      --bg-sidebar: rgba(255, 255, 255, 0.85);
      --text-primary: #1D1D1F;
      --text-secondary: #86868B;
      --accent: #C44444; /* VERMELHO DA SUA MARCA (Extraído do Logo) */
      
      /* Efeitos */
      --shadow-sm: 0 4px 12px rgba(0, 0, 0, 0.03);
      --shadow-md: 0 12px 32px rgba(196, 68, 68, 0.08); /* Sombra avermelhada suave */
      --glass-blur: blur(20px) saturate(180%);
      --radius-lg: 24px;
      --transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    
    body {
      font-family: 'Inter', sans-serif;
      color: var(--text-primary);
      background-color: var(--bg-body);
      -webkit-font-smoothing: antialiased;
      display: flex;
      min-height: 100vh;
    }

    /* --- Sidebar --- */
    aside {
      width: var(--sidebar-width); height: 100vh; position: fixed; left: 0; top: 0;
      padding: 60px 32px; background: var(--bg-sidebar);
      backdrop-filter: var(--glass-blur); -webkit-backdrop-filter: var(--glass-blur);
      border-right: 1px solid rgba(0,0,0,0.05);
      display: flex; flex-direction: column; z-index: 50;
    }

    .profile-container { text-align: center; margin-bottom: 40px; }
    .profile-pic-wrapper { width: 120px; height: 120px; margin: 0 auto 20px; border-radius: 50%; overflow: hidden; box-shadow: var(--shadow-sm); transition: transform 0.3s ease; }
    .profile-pic-wrapper:hover { transform: scale(1.03); }
    .profile-pic { width: 100%; height: 100%; object-fit: cover; }
    .profile-name { font-size: 1.5rem; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 6px; color: #111; }
    .profile-role { font-size: 0.8rem; color: var(--text-secondary); font-weight: 500; text-transform: uppercase; letter-spacing: 0.05em; }

    nav { flex: 1; display: flex; flex-direction: column; gap: 24px; }
    .nav-group h3 { font-size: 0.75rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 12px; opacity: 0.8; }
    .nav-list { list-style: none; }
    .nav-list li { margin-bottom: 4px; }
    .nav-link { display: block; padding: 8px 12px; color: var(--text-secondary); text-decoration: none; font-size: 0.95rem; font-weight: 500; border-radius: 8px; transition: all 0.2s; }
    /* Hover Vermelho para esta página */
    .nav-link:hover { color: var(--accent); background: rgba(196, 68, 68, 0.05); }
    
    .sidebar-footer { margin-top: auto; padding-top: 20px; border-top: 1px solid rgba(0,0,0,0.05); }
    .social-icons { display: flex; justify-content: center; gap: 12px; margin-bottom: 16px; }
    .social-icon { width: 36px; height: 36px; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(0,0,0,0.04); color: var(--text-secondary); text-decoration: none; transition: all 0.3s; }
    .social-icon:hover { background: var(--accent); color: white; transform: translateY(-3px); }
    .copyright { font-size: 0.7rem; color: #ccc; text-align: center; }

    /* --- Project Content --- */
    main {
      margin-left: var(--sidebar-width);
      flex: 1; padding: 0;
      display: flex; flex-direction: column; align-items: center;
    }

    /* Hero Section - Clean & Bold */
    .project-hero {
      width: 100%; height: 65vh; min-height: 450px; position: relative; 
      background-color: #C44444; /* Vermelho da marca */
      display: flex; align-items: center; justify-content: center;
      overflow: hidden;
    }

    .hero-image {
      width: 100%; height: 100%; object-fit: cover;
      opacity: 1; transition: transform 0.7s ease;
    }
    .hero-image:hover { transform: scale(1.02); }

    .hero-overlay {
      position: absolute; inset: 0;
      background: linear-gradient(to top, rgba(0,0,0,0.4), transparent);
      display: flex; align-items: flex-end; padding: 60px;
    }

    .container { width: 100%; max-width: var(--max-width); margin: 0 auto; }
    
    .project-title { font-size: 4rem; font-weight: 700; color: white; margin-bottom: 10px; letter-spacing: -0.04em; }
    .project-subtitle { font-size: 1.3rem; color: rgba(255,255,255,0.95); font-weight: 400; letter-spacing: 0.02em; }

    .content-wrapper { padding: 80px 60px; width: 100%; max-width: var(--max-width); }

    /* Meta Info Styles */
    .meta-grid {
      display: flex; gap: 60px; margin-bottom: 60px;
      border-bottom: 1px solid rgba(0,0,0,0.08); padding-bottom: 40px;
    }
    .meta-item h4 { font-size: 0.75rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 6px; }
    .meta-item p { font-size: 1rem; font-weight: 600; color: var(--text-primary); }

    /* Narrative Text */
    .project-description { 
      font-size: 1.35rem; line-height: 1.6; color: #333; margin-bottom: 100px; max-width: 800px;
    }
    .project-description strong { color: var(--accent); font-weight: 600; }

    /* Section Titles */
    .section-header { text-align: center; margin-bottom: 60px; }
    .section-badge { display: inline-block; padding: 6px 16px; border-radius: 50px; background: rgba(196, 68, 68, 0.1); color: var(--accent); font-size: 0.8rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 16px; }
    .section-title { font-size: 2.2rem; font-weight: 700; letter-spacing: -0.02em; }

    /* LOGO GRID (The "Brand Book" Look) */
    .logo-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-bottom: 100px;
    }

    .logo-card {
      aspect-ratio: 1/1;
      display: flex; align-items: center; justify-content: center;
      border-radius: var(--radius-lg);
      padding: 40px;
      transition: var(--transition);
      cursor: zoom-in;
      border: 1px solid rgba(0,0,0,0.05);
    }

    .logo-card:hover { transform: translateY(-5px); box-shadow: var(--shadow-md); }
    
    .logo-card img { width: 60%; height: auto; transition: transform 0.4s ease; }
    .logo-card:hover img { transform: scale(1.1); }

    /* Background Variations */
    .bg-white { background-color: #ffffff; }
    .bg-black { background-color: #111111; }
    .bg-red { background-color: #C44444; }
    .bg-gray { background-color: #F5F5F7; }

    /* Navigation */
    .project-nav {
      padding: 60px 0; border-top: 1px solid rgba(0,0,0,0.08);
      display: flex; justify-content: space-between; align-items: center;
    }
    .nav-btn {
      text-decoration: none; display: flex; align-items: center; gap: 12px;
      color: var(--text-secondary); font-weight: 500; transition: 0.3s;
    }
    .nav-btn:hover { color: var(--accent); }
    .nav-btn.next { text-align: right; flex-direction: row-reverse; }
    .nav-project-name { font-size: 1.2rem; font-weight: 700; color: var(--text-primary); }

    /* Lightbox Styles (Standard) */
    .lightbox {
      position: fixed; inset: 0; z-index: 1000;
      background: rgba(255, 255, 255, 0.95); /* Light theme for this brand */
      backdrop-filter: blur(10px);
      display: flex; justify-content: center; align-items: center;
      opacity: 0; pointer-events: none; transition: opacity 0.3s ease;
    }
    .lightbox.active { opacity: 1; pointer-events: auto; }
    .lb-image { max-width: 85vw; max-height: 85vh; border-radius: 12px; box-shadow: 0 20px 60px rgba(0,0,0,0.15); }
    .lb-control {
      position: absolute; background: rgba(0,0,0,0.05); color: #333;
      width: 50px; height: 50px; border-radius: 50%; border: none;
      font-size: 1.2rem; cursor: pointer; transition: 0.2s;
    }
    .lb-control:hover { background: var(--accent); color: white; }
    .lb-close { top: 30px; right: 30px; } .lb-prev { left: 30px; } .lb-next { right: 30px; }

    /* Responsividade */
    @media (max-width: 968px) {
      body { flex-direction: column; }
      aside { width: 100%; height: auto; position: relative; padding: 20px; border-right: none; flex-direction: row; justify-content: space-between; align-items: center; }
      .profile-role, nav, .sidebar-footer { display: none; }
      .profile-container { margin: 0; text-align: left; display: flex; align-items: center; gap: 12px; }
      .profile-pic-wrapper { width: 48px; height: 48px; margin: 0; }
      .profile-name { font-size: 1.1rem; margin: 0; }
      main { margin-left: 0; }
      .project-title { font-size: 2.5rem; }
      .content-wrapper { padding: 40px 20px; }
      .logo-grid { grid-template-columns: 1fr; }
      .meta-grid { flex-direction: column; gap: 20px; }
    }
  </style>
</head>
<body>

  <aside>
    <div class="profile-container">
      <div class="profile-pic-wrapper">
        <img src="Logo.png" alt="Gabriel Silva" class="profile-pic">
      </div>
      <h1 class="profile-name">Gabriel Silva</h1>
      <p class="profile-role">Designer & Produtor de Vídeo</p>
    </div>

    <nav>
      <div class="nav-group">
        <ul class="nav-list">
          <li><a href="sobre.html" class="nav-link">Sobre mim</a></li>
        </ul>
      </div>

      <div class="nav-group">
        <h3>Portfólio</h3>
        <ul class="nav-list">
          <li><a href="index.html" class="nav-link active">← Voltar para Projetos</a></li>
        </ul>
      </div>
    </nav>

    <div class="sidebar-footer">
      <div class="social-icons">
        <a href="https://www.linkedin.com/in/ogabriels/" target="_blank" class="social-icon"><i class="fab fa-linkedin-in"></i></a>
        <a href="https://www.instagram.com/ogabriels0/" target="_blank" class="social-icon"><i class="fab fa-instagram"></i></a>
        <a href="mailto:contato@ogabriels.com" class="social-icon"><i class="far fa-envelope"></i></a>
        <a href="https://wa.me/5511979686116" target="_blank" class="social-icon"><i class="fab fa-whatsapp"></i></a>
      </div>
      <p class="copyright">© 2026 Gabriel Silva</p>
    </div>
  </aside>

  <main>
    
    <section class="project-hero">
      <img src="Gorpos - capa.png" alt="G | Gabriel Branding" class="hero-image lightbox-trigger">
      <div class="hero-overlay">
        <div class="container">
          <h1 class="project-title">G | Gabriel</h1>
          <p class="project-subtitle">Personal Branding & Identidade Visual</p>
        </div>
      </div>
    </section>

    <div class="content-wrapper">
      
      <div class="meta-grid">
        <div class="meta-item"><h4>Projeto</h4><p>Marca Pessoal</p></div>
        <div class="meta-item"><h4>Ano</h4><p>2025</p></div>
        <div class="meta-item"><h4>Conceito</h4><p>Minimalismo & Conexão</p></div>
      </div>

      <section class="project-description">
        <p>A construção de uma marca pessoal exige a destilação de uma identidade complexa em um símbolo singular. O projeto <strong>G | Gabriel</strong> surgiu da busca por um ícone que fosse, ao mesmo tempo, memorável e funcional.</p>
        
        <p>O "G" representa minha inicial e serve como âncora da marca. Ele é integrado a uma forma curva sutil na base, remetendo a um <strong>sorriso estilizado</strong>. Essa escolha de design não é acidental: ela traz o equilíbrio necessário entre o profissionalismo corporativo e a acessibilidade humana.</p>

        <p>Essa marca é a assinatura visual da minha abordagem ao design: <strong>direta, funcional e visualmente impactante.</strong></p>
      </section>

      <section class="showcase-section">
        <div class="section-header">
          <span class="section-badge">Sistema Visual</span>
          <h2 class="section-title">Variações & Aplicação</h2>
        </div>

        <div class="logo-grid">
          
          <div class="logo-card bg-white">
            <img src="Logotipo G (ogabriels) vermelho.png" alt="Logo Vermelho Fundo Branco" class="lightbox-trigger">
          </div>

          <div class="logo-card bg-red">
            <img src="Logotipo G (ogabriels) monocromático negativo.png" alt="Logo Branco Fundo Vermelho" class="lightbox-trigger">
          </div>

          <div class="logo-card bg-gray">
            <img src="Logotipo G (ogabriels) monocromático.png" alt="Logo Preto Fundo Cinza" class="lightbox-trigger">
          </div>

          <div class="logo-card bg-black">
            <img src="Logotipo G (ogabriels) vermelho negativo.png" alt="Logo Vermelho Fundo Preto" class="lightbox-trigger">
          </div>

        </div>
      </section>

    </div>

    <div class="project-nav">
      <a href="lo.html" class="nav-btn prev">
        <i class="fa-solid fa-arrow-left"></i>
        <div><span class="nav-label">Anterior</span><span class="nav-project-name">Lô</span></div>
      </a>
      
      <a href="um-minuto.html" class="nav-btn next">
        <div><span class="nav-label">Próximo</span><span class="nav-project-name">Um Minuto para Refletir</span></div>
        <i class="fa-solid fa-arrow-right"></i>
      </a>
    </div>

  </main>

  <div id="lightbox" class="lightbox">
    <button id="lb-close" class="lb-control lb-close"><i class="fa-solid fa-xmark"></i></button>
    <div class="lb-image-container">
      <img src="" alt="Zoom" id="lb-image" class="lb-image">
    </div>
  </div>

  <script>
    const triggers = document.querySelectorAll('.lightbox-trigger');
    const lightbox = document.getElementById('lightbox');
    const lbImage = document.getElementById('lb-image');
    const lbClose = document.getElementById('lb-close');

    function openLightbox(src) {
      lbImage.src = src;
      lightbox.classList.add('active');
      document.body.style.overflow = 'hidden';
    }

    function closeLightbox() {
      lightbox.classList.remove('active');
      document.body.style.overflow = '';
      setTimeout(() => { lbImage.src = ''; }, 300);
    }

    triggers.forEach(img => {
      img.addEventListener('click', () => openLightbox(img.src));
    });

    lbClose.addEventListener('click', closeLightbox);
    lightbox.addEventListener('click', (e) => {
      if(e.target === lightbox) closeLightbox();
    });
    document.addEventListener('keydown', (e) => {
      if(e.key === 'Escape') closeLightbox();
    });
  </script>

</body>
</html>